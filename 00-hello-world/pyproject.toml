[project]
name = "hello-world"
version = "0.1.0"
description = "Part 0: Verify SAP Generative AI Hub connectivity"
requires-python = ">=3.12"
dependencies = [
    "python-dotenv>=1.0.0",
    "sap-ai-sdk-gen[langchain]>=1.0.0",
]

[tool.uv]
dev-dependencies = []
